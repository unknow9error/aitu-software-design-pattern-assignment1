# Подготовка к защите

## Основная идея

Builder позволяет собирать объект по шагам. Здесь сначала задаётся заголовок,
затем добавляются разделы, а вызов `build()` создаёт готовый отчёт.
В этой упрощённой версии один builder создаёт отчёт в обычном текстовом виде.

В программе четыре класса:

| Класс | Назначение |
| --- | --- |
| `Report` | Готовый отчёт: заголовок и содержимое. Getters позволяют прочитать эти поля. |
| `ReportBuilder` | Хранит заголовок и накопленные разделы, проверяет данные, задаёт шаги сборки. |
| `ReportDirector` | Содержит готовый рецепт: заголовок и три раздела. |
| `Main` | Запускает программу и печатает отчёты, собранные с Director и без него. |

## Как читать код

**`class`** описывает, какие данные и методы будут у объекта.
Например, у `Report` есть поля `title`, `content` и методы `getTitle()`,
`getContent()`. Через `new` создаётся конкретный объект этого класса.

**`new ReportBuilder()`** создаёт объект для сборки отчёта. Сначала его заголовок
и разделы пусты. `setTitle()` записывает заголовок, а `addSection()` добавляет
к накопленному тексту ещё один раздел. В строках `\n` означает перенос строки.

**`return this`** возвращает текущий builder. Поэтому после одного вызова
сразу можно вызвать следующий — это fluent API, или цепочка методов:

```java
Report report = new ReportBuilder()
        .setTitle("My homework")
        .addSection("Task", "Practice method chaining")
        .build();
```

**`new Report(...)`** внутри `build()` создаёт готовый продукт из собранных
данных. `setTitle()` и `addSection()` возвращают builder, а `build()` — уже `Report`.

## Демонстрация за 3 минуты

Перед демонстрацией открыть папку проекта в IntelliJ IDEA, выбрать JDK 17
в качестве Project SDK и отметить `src/main/java` через
**Mark Directory as → Sources Root**.

1. **0:00–0:45.** Открыть `Main.java` и запустить `Main.main()` кнопкой Run.
   Показать учебный отчёт и отдельный отчёт без Director в консоли.
2. **0:45–1:30.** Показать в `ReportDirector.java` готовый рецепт,
   а в `Main.java` — самостоятельную цепочку методов.
3. **1:30–2:15.** Открыть `ReportBuilder.java` и `Report.java`.
   Объяснить накопление разделов, `return this` и `new Report(...)`.
4. **2:15–3:00.** Показать условия в `checkText()` и `build()`.
   Объяснить, почему пустые данные отклоняются, а отчёт без заголовка
   или разделов нельзя собрать.

## Короткие ответы на вопросы

**Зачем Builder, если у `Report` всего два поля?**
Сам объект простой, но его содержимое собирается из нескольких разделов.
На этом примере видны пошаговая сборка и цепочка методов.
Для отчёта с заранее готовым содержимым достаточно обычного конструктора.

**Зачем Director?**
Он сохраняет последовательность шагов для стандартного учебного отчёта.
Он необязателен: в `Main` есть самостоятельная цепочка без него.

**Где проверки данных?**
`checkText()` отклоняет `null`, пустые строки и строки из пробелов.
`build()` требует заголовок и хотя бы один раздел.

**Можно повторно использовать builder?**
Да, но `build()` не очищает его разделы. Для нового отчёта в примере создаётся
новый builder. Каждый `build()` возвращает отдельный объект `Report`.

## Перед сдачей

Исходное задание требует Builder в виде интерфейса или абстрактного класса
и минимум два конкретных builders. Эта намеренно упрощённая версия с одним
обычным `ReportBuilder` **не выполняет эти два требования**.

Согласование свободной темы с преподавателем здесь не подтверждено.
Ссылку на GitHub и PDF нужно отдельно отправить в Moodle.
